---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<button
  class:list={[
    "px-4 py-2 text-sm shadow border border-orange-600 hover:border-orange-500 rounded-lg bg-orange-500 hover:bg-orange-400 text-white",
    className,
  ]}
>
  <slot />
</button>
